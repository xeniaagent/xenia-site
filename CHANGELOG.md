# Changelog

A human-readable history of changes to the Xenia Brown SF real estate site. For the full commit-by-commit record, see `git log` or the GitHub commits page. For a single-day snapshot, see `docs/SESSION-YYYY-MM-DD.md`.

---

## 2026-06-22

See `docs/SESSION-2026-06-22.md` for the full session log. Headline changes:

- **SF neighborhood map** added to the hero (interactive, 41 polygons + 27 labels)
- **Chart slider** added to the neighborhood panel (24-month scrub + readout in heading row)
- **Office address** linked to Google Maps in two places (profile photo + Let's talk)
- **Lone Mountain/USF** naming fixed across all touchpoints
- **Contact form** fixed: replaced the fake-success stub with `mailto:` (Formspree pending)
- **Map visual spec** settled at 60% opacity, white labels, bright orange hover
